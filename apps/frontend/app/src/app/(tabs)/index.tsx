import { ScrollView, View } from 'react-native';

export default function ProfileScreen() {
  return (
    <ScrollView contentContainerClassName="flex-1 justify-center">
      <View className="bg-red-600 h-12 w-12 mx-auto"></View>
    </ScrollView>
  );
}
